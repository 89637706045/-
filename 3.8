#include<stdio.h>
#include<stdlib.h>
#include<math.h>
int main()
{
	float a=0, b=0, a1=a, b1=b, x, prod, znachenye[3], c=0, znach;
	puts("Vvedite znaychenia konstov otrezka, snachalo min, a potom max");
	scanf("%f%f", &a, &b);
	/*printf("%f %f\n", a, b);*/
	// rasschet"
	znach=pow(10, -10);
	znachenye[0]=tan(a)-a;
	znachenye[1]=tan(b)-b;
	while(prod==1)
	{
		c=a1+((b1-a1)/2);
		znachenye[2]=tan(c)-c;
		if(znachenye[0]*znachenye[2]<0)
		{
			b1=c;
		}
		else
		{
			
			a1=c;
		}
		if((b1-a1)>=znach)
		{
			prod=1;	
		}
		else
		{
			prod=0;
		}
	}
	x=tan(c)-c;
	printf("%f", x);
	return 0;
}
