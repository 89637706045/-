#include<stdio.h>//решить зад фиб рекур
#include<stdlib.h>



int f(int n)
{
	if (n<3)
	{
		return 1;
	}
	else
	{
		return f(n-1)+f(n-2);
	}
}

int main()
{
	int n;
	scanf("%i", &n);
	int ans = f(n);// здесь произошел вызов функции  "int f(int n)"
	printf("ans = %d", ans);
}
